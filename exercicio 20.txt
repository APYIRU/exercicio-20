1-
num = input("Digite uma lista de números separados por espaços: ")
lista = [float(n) for n in num.split()]
resultado = sum(lista)
print(f"A soma de todos os númeors é: {resultado}")

2-
num = input("Digite uma lista de valores booleanos (true ou false) separados por espaços: ")
lista = [valor.lower() == 'true' for valor in num.split()]
if all(lista) == True:
    print("Todos valores da lista são verdadeiros")
else:
    print("A lista contém um ou mais elementos falsos")

3-
num = input("Digite qualquer coisa: ")
def entrada(num):
    if num.lower() == 'true' or num.lower() == 'false':
        return "Boleana"
    elif num.isdigit() == True:
        return "Inteiro"
    else:
        try:
            float(num)
            return "Float"
        except ValueError:
            return "String"
resposta = entrada(num)
print(f"A entrada {num} é do tipo: {resposta}")


4-
num = input("Digite um texto: ")
quant = len(num.replace(" ",""))
print(f"O seu texto tem {quant} caracteres")

5-
num = input("Digite uma lista de números separadas por espaço: ")
lista = [float(n) for n in num.split()]
maxi = max(lista)
print(f"O maior número da lista é: {maxi}")

6-
num = input("Digite uma lista de números separadas por espaço: ")
lista = [float(n) for n in num.split()]
maxi = min(lista)
print(f"O maior número da lista é: {maxi}")

7-
num = int(input("Digite um número negativo: "))
if num > -1:
    print("Número inválido")
else:
    num2 = num * -1
    print(f"O valor absoluto de {num} é: {num2}")

8-
num = input("Digite uma lista de números separados por espaços: ")
lista = [float(n) for n in num.split()]
lista.sort()
print(f"Os números ordenados de forma crescente ficarão desse jeito: {lista}")

9-
for i in range(0,11):
    print(i)

10-
num = input("Digite um texto para ser invertido: ")
num2 = num[::-1]
print(num2)

11-
num = input("Digite uma lista de valores booleanos (true ou false) separados por espaços: ")
lista = [valor.lower() == 'true' for valor in num.split()]
if not any(lista) == True:
    print("Todos valores da lista são falsos")
else:
    print("A lista contém um ou mais elementos verdadeiros")
